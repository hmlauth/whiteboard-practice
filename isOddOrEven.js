// isAnOddNumberWithAnOddFunctionName which accepts a number and returns true if it's an odd number or false if it's even.

function isAnOddNumberWithAnOddFunctionName(num) {
    if (num%2 === 0) {
        return false
    } else {
        return true
    }
}
